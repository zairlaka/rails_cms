<!DOCTYPE html>
<html>
    <head>
        <title>Simple CMS: <%= @page_title || "Admin Area" %></title>
        <%= stylesheet_link_tag('admin', :media => 'all') %>    
    </head>
    <body>

        <div id="header">
            <span class='left'><%= link_to("- Home Page", root_path) %></span>
            <h1>Simple CMS Admin </h1>
            <span class='right'><%= link_to("Admin Page -", access_menu_path) %></span>
        </div>
        <div id="main">
            <% if !flash[:notice].blank?  # its check for the nil but also for the empty strings %>
                <div class= "notice">
                    <%= flash[:notice] %>
                </div>
            <% end %>
            <% if !flash[:alert].blank?  # its check for the nil but also for the empty strings %>
                <div class= "error_notice">
                    <%= flash[:alert] %>
                </div>
            <% end %>
            <!-- after yeild --->
            <div id="content">
                <%= yield %>    
            </div>
            <!-- after yeild --->
        </div>
        <div id="footer">
            <p id="copyright"> &copy; cms.com </p>
        </div>
    </body>
</html>
