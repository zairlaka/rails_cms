<%= link_to("<< Back to List", sections_path(:page_id => @page.id), :class => 'back-link') %>

<div class="section edit">
  <h2>Update section</h2>

  <%= form_for(@section, :url => section_path(@section, :page_id => @page), :method => 'patch') do |f| %>
  <%# form_for(@section) do |f|  #we dont have to specify them becuase you are post something and see rails route %>    

    <%= render(:partial => "form" , :locals => {:f => f}) %>


    <div class="form-buttons">
      <%= f.submit("Update Section") %>
    </div>

  <% end %>
</div>
